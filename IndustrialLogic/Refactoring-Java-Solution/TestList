Test List:

Write test that creates an Account successfully with fewest required fields, verifies Account 
has been created(in both Dashboard tab and Accounts tab), deletes the Account, and 
verifies the Account no longer exists. 

Write test that creates an Opportunity, and verifies it has been created (including Opportunity tab an d
Dashboard tab), deletes it, and verifies it no longer exists. 

Write a test that verifies that on the Opportunities tab, each of the checkbox filters works 
(involves creating Opportunities for each of the filter types). 

Write a test that creates a Campaign, verifies it has been created, deletes it, and 
verifies it no longer exists. 

Write a test that verifies that on the Campaigns tab, each of the checkbox filters works 
(involves creating Campaigns for each of the filter types). 


